package ru.hogwarts.school.service;

import org.junit.jupiter.api.Test;
import ru.hogwarts.school.model.Student;

import java.util.Collection;

import static org.junit.jupiter.api.Assertions.*;

class StudentServiceTest {
  /*  StudentService service= new StudentService();

    @Test
    void addStudent() {
        var actual=service.addStudent(new Student(1L,"fgg",15));
        assertEquals(1,actual.getId());
    }

    @Test
    void findStudent() {
        var expected=service.addStudent(new Student(1L,"fgg",15));
        assertEquals(expected,service.findStudent(1L));
    }

    @Test
    void editStudent() {
        service.addStudent(new Student(1L,"fgg",15));
       var actual= service.editStudent(new Student(1L,"jjhrfjgh",15));
        assertEquals(1,actual.getId());
    }

    @Test
    void removeStudent() {
        service.addStudent(new Student(1L,"uvhu",12));
        service.addStudent(new Student(2L,"jkf",15));
        assertEquals(2,service.getAll().size());
        service.removeStudent(2L);
        assertEquals(1,service.getAll().size());
    }

    @Test
    void getAll() {
        service.addStudent(new Student(1L,"uvhu",12));
        service.addStudent(new Student(2L,"jkf",15));
        Collection<Student>students=service.getAll();
        assertEquals(2,students.size());
    }

    @Test
    void getStudentAge() {
       var actual= service.addStudent(new Student(2L,"jkf",15));
       assertEquals(15,actual.getAge());

    }*/
}